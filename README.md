# 🚀 AI Workshop Team Submission & Showcase Platform
**ระบบเว็บส่งงานและแสดงผลงานแข่งขันระหว่างทีม สำหรับกิจกรรมการอบรม / เวิร์กชอป**

---

## 🎨 ภาพรวมการออกแบบ (AI Workshop Theme)
- **ธีมสี**: น้ำเงิน AI Workshop (`#1E5AF6`), น้ำเงินเข้ม (`#0B1B3D`), สีฟ้าสว่าง (`#93C5FD`), และพื้นหลังคลีนมินิมอลลายจุด
- **มาสคอต**: น้องก้อนสีน้ำเงินตาโต (Blue Blobs) ถือหลอดไฟ พิมพ์โน้ตบุ๊ก ปล่อยประกายดาว ✨ ตามอาร์ตเวิร์กต้นฉบับ
- **ฟอนต์**: ภาษาไทยโมเดิร์น (Prompt / Kanit) อ่านง่าย ชัดเจน ปุ่มกดขนาดใหญ่สัมผัสง่ายบนมือถือ

---

## 📱 1. หน้าสำหรับผู้เข้าอบรม (Mobile Participant View)
- **URL**: `http://<IP_เครื่องวิทยากร>:3000` (หรือสแกน QR Code จากหน้าจอวิทยากร)
- **การใช้งาน**:
  1. เข้าสู่หน้าเว็บผ่านมือถือ (หน้าเดียว ไม่ต้องล็อกอิน)
  2. แตะเลือก **ทีมของตนเอง** (เช่น ทีม A, ทีม B, ทีม C, ทีม D)
  3. แตะเลือก **ไฟล์รูปภาพ** (หรือถ่ายภาพสด) หรือไฟล์เอกสาร (PDF, สไลด์)
  4. ใส่ชื่อผู้ส่ง / สมาชิก และคำอธิบายไอเดียหรือ Prompt (ไม่บังคับ)
  5. กดปุ่ม **"ส่งผลงานเลย! 🚀"**
  6. แสดงแอนิเมชัน Confetti 🎉 พร้อมการ์ดยืนยันการส่งงานสำเร็จ และดูประวัติการส่งงานของทีมตนเองได้ทันที

---

## 🖥️ 2. หน้าจอวิทยากร & โปรเจกเตอร์ (Speaker Dashboard)
- **URL**: `http://localhost:3000/speaker.html`
- **ฟีเจอร์สำคัญสำหรับวิทยากร**:
  - 📱 **QR Code ส่งงาน**: กดเปิดปุ่ม QR Code ขนาดใหญ่ขึ้นจอโปรเจกเตอร์ ระบบตรวจจับ Local IP ให้อัตโนมัติ เพื่อให้ผู้เข้าอบรมในวง Wi-Fi สแกนเข้าได้ทันที
  - 🔒/👁️ **สวิตช์เปิด/ปิด โชว์ผลงาน (Toggle Reveal Mode)**:
    - **โหมดซ่อนผลงาน (ระหว่างแข่งขัน)**: บนจอจะขึ้นแค่สถานะว่าทีมไหนส่งแล้วกี่ชิ้น แต่รูปภาพและไอเดียจะถูกล็อกและซ่อนไว้ เพื่อไม่ให้ทีมอื่นลอกผลงาน
    - **โหมดเปิดโชว์ผลงาน (ช่วงนำเสนอ)**: ปลดล็อกรูปภาพทั้งหมด แสดงเป็น Gallery คมชัด พร้อมชื่อทีมและ Prompt
  - 🔄 **ปุ่มรีเฟรช & Live Auto-Update**: อัปเดตข้อมูลอัตโนมัติทุก 4 วินาที หรือกดปุ่มรีเฟรชได้ทันที
  - 🔍 **Presentation Mode (สไลด์โชว์เต็มจอ)**: คลิกที่ผลงานเพื่อเปิดดูแบบเต็มจอ พร้อมปุ่ม Previous / Next (หรือกดลูกศรบนคีย์บอร์ด `←` / `→`) เพื่อเดินนำเสนอผลงานทีละทีม
  - ❤️ **ระบบโหวต / ถูกใจ**: วิทยากรหรือผู้เข้าร่วมสามารถกดหัวใจให้คะแนนผลงานแต่ละชิ้นได้
  - ⚙️ **ตั้งค่าเวิร์กชอป (Settings)**: แก้ไขชื่อหัวข้อ, ภารกิจ, ปรับจำนวนทีม / เพิ่มทีม / เปลี่ยนสีทีม, และตั้งขนาดไฟล์สูงสุด
  - 📥 **ดาวน์โหลดทั้งหมด (ZIP)**: บันทึกไฟล์ผลงานทั้งหมดแยกตามโฟลเดอร์ทีม พร้อมสรุปข้อมูลเป็นไฟล์เอกสาร
  - 🗑️ **รีเซ็ตเริ่มรอบใหม่**: เคลียร์ผลงานเพื่อเริ่มกิจกรรมข้อใหม่ (มีระบบสำรองข้อมูลอัตโนมัติ)

---

## 🚀 วิธีการเปิดใช้งานระบบ (How to Run)

### ขั้นตอนการรัน:
```bash
cd /Users/dollawat_mac/.gemini/antigravity/scratch/workshop-submission-app
npm start
```

- **หน้าส่งงาน (มือถือ)**: `http://localhost:3000` (หรือ `http://192.168.1.X:3000`)
- **หน้าจอวิทยากร (คอมพิวเตอร์ / โปรเจกเตอร์)**: `http://localhost:3000/speaker.html`

### การตั้งค่า Cloud Persistence บน Vercel

ระบบ production ต้องมี Environment Variables ต่อไปนี้ใน Vercel Project Settings:

- `GITHUB_TOKEN`: GitHub token ที่มีสิทธิ์อ่านและเขียนไฟล์ Contents ของ repository
- `GITHUB_REPO`: ค่าเริ่มต้นคือ `dollawat1234/workshop-submission-app` หากใช้ repository อื่นให้กำหนดค่านี้เพิ่ม
- `GITHUB_BOOTSTRAP`: ตั้งเป็น `true` เฉพาะตอนเริ่ม repository ใหม่และต้องการสร้าง `data/store.json` จาก seed ใน repo; หลัง bootstrap ควรลบหรือปิดค่าเป็น `false`
- `STORAGE_ROOT`: โฟลเดอร์รากสำหรับข้อมูลถาวรของ Docker เช่น `/var/lib/teamgame`; ระบบจะใช้ `${STORAGE_ROOT}/data` และ `${STORAGE_ROOT}/uploads`
- `SPEAKER_KEY`: รหัสลับสำหรับหน้าวิทยากรและ API ที่แก้ไข/ลบ/รีเซ็ตข้อมูล
- `REQUIRE_SPEAKER_AUTH`: ตั้งเป็น `true` ใน production เพื่อบังคับรหัสวิทยากร; ค่าเริ่มต้นของ local development ปิดไว้

ห้ามใส่ token ลงใน source code หรือ commit ลง Git หากไม่มี `GITHUB_TOKEN` ระบบจะตอบสถานะ `503` เพื่อป้องกันการรับงานสำเร็จหลอก ๆ แล้วข้อมูลหายเมื่อเปลี่ยน Serverless Instance หาก token เคยถูก commit หรือเผยแพร่แล้ว ควร revoke และสร้าง token ใหม่ก่อน deploy

ทุกการแก้ไขข้อมูลใช้ GitHub file SHA แบบ compare-and-swap และ retry/rebase เมื่อมีผู้ใช้หลายคนส่งพร้อมกัน ส่วนการส่งไฟล์ใช้ idempotency key เพื่อให้การกดส่งซ้ำหลังสัญญาณขัดข้องไม่สร้างผลงานซ้ำ

### การ Deploy บน Hostinger VPS

สำหรับ Docker ที่มี Persistent Volume ให้กำหนด `STORAGE_ROOT=/var/lib/teamgame` และ mount โฟลเดอร์บนเครื่องจริงไปยัง `/var/lib/teamgame` ใน container ห้ามเก็บข้อมูลสำคัญไว้เฉพาะใน image เพราะไฟล์จะถูกแทนที่เมื่อ build ใหม่

ระบบจะสร้าง Full Backup เป็นไฟล์ ZIP ก่อนคำสั่ง Reset โดยรวม `snapshot.json`, `manifest.json` และไฟล์ใน `uploads` ที่ถูกอ้างอิงอยู่ หากสร้าง Backup ไม่ครบ ระบบจะไม่ดำเนินการ Reset เพื่อป้องกันการลบไฟล์โดยไม่มีสำเนา

สามารถสร้าง Backup ของ Persistent Volume ได้ด้วย `bash ops/backup.sh` ซึ่งจะเก็บทั้ง `data` และ `uploads` ไว้ในโฟลเดอร์ `backups`
